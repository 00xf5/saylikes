ADMIN_TOKEN=your-strong-password-here
# Neon connection string (required for devices to persist)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
# Optional — only needed to upload how-to videos
BLOB_READ_WRITE_TOKEN=
